<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hiennv.flutter_callkit_incoming">

    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.TURN_SCREEN_ON" />

    <!-- Needed to detect a real (cellular/other VoIP) incoming call while an
         order ring is active, so the order ring is dismissed immediately and
         does not overlap the phone call. The host app must request this
         dangerous permission at runtime; when not granted the ring simply
         plays to completion (it is a notification now, not a real Telecom call,
         so it can no longer block the incoming call). -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-permission android:name="${applicationId}.PERMISSION_CALL" />

    <permission
        android:name="${applicationId}.PERMISSION_CALL"
        android:protectionLevel="normal" />

    <application>

        <activity
            android:name="com.hiennv.flutter_callkit_incoming.CallkitIncomingActivity"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:turnScreenOn="true"
            android:configChanges="orientation"
            android:exported="true"
            android:launchMode="singleInstance"
            android:allowTaskReparenting="false"
            android:theme="@style/CallkitIncomingTheme"
            android:permission="${applicationId}.PERMISSION_CALL">
            <intent-filter>
                <action android:name="${applicationId}.com.hiennv.flutter_callkit_incoming.ACTION_CALL_INCOMING" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:exported="false"
            android:theme="@style/TranslucentTheme"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:name="com.hiennv.flutter_callkit_incoming.TransparentActivity"/>

        <receiver
            android:name="com.hiennv.flutter_callkit_incoming.CallkitIncomingBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="${applicationId}.PERMISSION_CALL">
            <intent-filter>
                <action android:name="${applicationId}.com.hiennv.flutter_callkit_incoming.ACTION_CALL_INCOMING" />
                <action android:name="${applicationId}.com.hiennv.flutter_callkit_incoming.ACTION_CALL_ACCEPT" />
                <action android:name="${applicationId}.com.hiennv.flutter_callkit_incoming.ACTION_CALL_DECLINE" />
                <action android:name="${applicationId}.com.hiennv.flutter_callkit_incoming.ACTION_CALL_ENDED" />
                <action android:name="${applicationId}.com.hiennv.flutter_callkit_incoming.ACTION_CALL_TIMEOUT" />
            </intent-filter>
        </receiver>

        <!--
            The order ring is a foreground service so it survives the keyguard
            and the ringtone/timeout keep running while the screen is locked.
            This is NOT a phone call: the plugin no longer registers a Telecom
            ConnectionService (that caused real cellular/VoIP calls to be
            blocked and the app to self-resurface). `specialUse` is the correct
            FGS type for a user-facing delivery-order alert; the subtype below
            is declared for the Play Console foreground-service declaration.
        -->
        <service
            android:enabled="true"
            android:exported="true"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse"
            android:permission="${applicationId}.PERMISSION_CALL"
            android:name="com.hiennv.flutter_callkit_incoming.CallkitNotificationService">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Incoming delivery order alert: plays a ringtone and shows a full-screen notification so the driver is promptly notified of a new assigned order." />
        </service>

    </application>
</manifest>
